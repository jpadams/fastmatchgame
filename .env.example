# The graph contains a PG(2,7) graph using emoji symbols.
# It's the authoritative, deterministic source of truth
# for which two symbols match between two cards.
NEO4J_URI=bolt://localhost:7687
NEO4J_USER=neo4j
NEO4J_PASSWORD=your-password

# Optional: AI player (vision model) - used when running AI opponent
# Uses gpt-4o-mini by default. Set OPENAI_BASE_URL for other OpenAI-compatible APIs.
OPENAI_API_KEY=
# Optional: override API base URL
# OPENAI_BASE_URL=
# Optional: model name (OPENAI_DEFAULT_MODEL or MODEL). Default: gpt-4o-mini
# OPENAI_DEFAULT_MODEL=
# Optional: max tokens for AI response. Default: 800
# OPENAI_MAX_TOKENS=800
